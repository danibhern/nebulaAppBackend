# ===============================================
# CONFIGURACIÓN PARA DESARROLLO LOCAL
# ===============================================
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/nebula_dbApp?serverTimezone=UTC
    username: root
    password: mypassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  # En desarrollo SÍ queremos ver SQL
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret-key: "clave-secreta-para-desarrollo-local-123456"
  expiration: 86400000