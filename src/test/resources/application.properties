# =========================================================
# CONFIGURACIÓN DE PRUEBAS (MAXIMA PRIORIDAD)
# Este archivo fuerza el uso de H2 en memoria para todos
# los tests, independientemente de otros perfiles.
# =========================================================

# Database H2 en memoria
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# Hibernate para Tests: Recrear esquema en cada test
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Desactivar la consola H2
spring.h2.console.enabled=false